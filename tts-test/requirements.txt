# Test-only tunnel server deps (ubuntu-latest runner, no GPU needed).
# FastAPI + uvicorn[standard] (uvloop/httptools) for high-concurrency async serving.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
youtube-transcript-api
