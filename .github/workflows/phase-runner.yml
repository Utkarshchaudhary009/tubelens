name: phase-runner

on: {}

permissions:
  contents: write
  pull-requests: write
  actions: read
  checks: read

concurrency:
  group: phase-runner-main
  cancel-in-progress: false

jobs:
  advance-phase:
    # No triggers are configured; this job is retained only as a disabled workflow stub.
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - run: echo "phase-runner disabled"
